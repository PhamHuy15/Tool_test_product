{
  "name": "local-ai-test-web-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "test": "node --test test/*.test.js",
    "check": "node --check index.js && node --check runCodex.js && node --check security.js && node --check artifacts.js",
    "lint": "npm run check",
    "setup:browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "express": "^4.19.2",
    "playwright": "^1.55.1"
  }
}
